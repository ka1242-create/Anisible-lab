---
- name: configure cisco devices 
  connection: network_cli
  gather_facts: false

  hosts: all

  tasks:
    - name: configuration commands on cisco device
      ios_config:
        lines:
          - "{{ item }}"
        multiline_delimiter: "\n"
      with_items: "{{ Commands_enter }}"
...
