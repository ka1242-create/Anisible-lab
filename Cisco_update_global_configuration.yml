---
- name: configure cisco devices 
  connection: network_cli
  gather_facts: false

  hosts: all

  tasks:
    - name: configuration commands on cisco device
      ios_config:
        lines:
          - "{{ item }}"
      loop: "{{ lookup(Commands_enter, wantlist=True) }}"
...
